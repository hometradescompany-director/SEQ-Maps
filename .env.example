# Google Cloud project used to provision Google Maps Platform services.
GOOGLE_CLOUD_PROJECT=

# Keep real values out of git. Use a server-restricted key for backend calls.
GOOGLE_MAPS_SERVER_API_KEY=

# Add only when a browser map surface exists, and restrict it by HTTP referrer.
GOOGLE_MAPS_BROWSER_API_KEY=
