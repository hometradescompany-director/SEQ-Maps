{
  "name": "seq-maps",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first mapping and traffic intelligence for Southeast Queensland.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --test",
    "gcp:maps:plan": "node scripts/gcp/enable-google-maps-platform.mjs --dry-run",
    "gcp:maps:enable": "node scripts/gcp/enable-google-maps-platform.mjs --apply"
  }
}
